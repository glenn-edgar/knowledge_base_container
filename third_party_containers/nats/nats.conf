port: 4222
http: 8222
server_name: nats-ram

# JetStream enabled; all data in RAM (tmpfs mounted at /js by run.sh)
jetstream: {
  domain: "RAM",
  store_dir: "/js",
  max_mem_store: 256Mb,
  max_file_store: 0
}

