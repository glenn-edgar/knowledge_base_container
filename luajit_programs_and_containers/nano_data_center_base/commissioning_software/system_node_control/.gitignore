# Per-CPU deployment artifacts live under deployment/<cpu>/.
# The deployment/ dir itself has its own .gitignore that ignores
# everything under it except README.md and the .gitignore itself.

# Compiled chain-tree IR (regenerated by runtime/chain_tree/build_dsl.sh).
runtime/chain_tree/dcs.json
runtime/chain_tree/dcs_debug.yaml
runtime/chain_tree/error.log
