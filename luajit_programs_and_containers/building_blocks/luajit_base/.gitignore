# Staged into the Docker build context by container/docker_build.sh.
# Rebuilt from sibling building_blocks every run; do not commit.
container/prebuilt_libs/
container/prebuilt_lua_libs/
container/prebuilt_lua_share/

# Compiled chain-tree IR. Regenerated by docker_build.sh from supervisor/dsl.lua.
container/supervisor/controller.json
container/supervisor/controller_debug.yaml
