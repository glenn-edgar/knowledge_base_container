/*
 * cfl_mp_bridge.c - MicroPython bridge for ChainTree CFL engine
 *
 * Status: stub — implementation follows after Lua 5.3 bridge is validated.
 *
 * Will mirror cfl_lua53_bridge.c with:
 *   - mp_obj_t trampoline dispatch
 *   - GC root registration via MP_STATE_PORT
 *   - "chaintree" MicroPython module definition
 */
