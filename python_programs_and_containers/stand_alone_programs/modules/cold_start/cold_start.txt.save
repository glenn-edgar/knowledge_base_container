determine the home directory
from pathlib import Path
home_dir = Path.home()
print(home_dir)

read ~/mount_startup/node_definition.json

    "site": "main"
    "node_name":"main",
    "master": true
    "update_flag":true

if file does
if master flag is false then exit or json fields do not exits then raise exceotion

if update_flag is false then exit

if update_flag is true 
then start containers postgres-vector  mosquitto-ram-ws_main
wait 10 seconds 
verify that the containers are running

verify that the directory ~/mount_kb_definitions exists
verify file  ~/mount_kb_definitions/start.py exists
run the script ~/mount_kb_definitison/start.py
mv ~/mount_kb_definitions/ to ~/mount_kb_definitions_completed

### To do load secrets and files
