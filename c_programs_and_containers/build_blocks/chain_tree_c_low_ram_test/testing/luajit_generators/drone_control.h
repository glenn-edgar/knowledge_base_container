// drone_control.h
// Generated by avro_dsl.lua - DO NOT EDIT
#pragma once

#include <stdint.h>
#include <stdbool.h>
#include <string.h>
#include "avro_common.h"

#ifdef __cplusplus
extern "C" {
#endif

// ============ FILE METADATA ============
#define DRONE_CONTROL_RECORD_COUNT  8
#define DRONE_CONTROL_SCHEMA_FILE   "drone_control.h"

// ============ USER POINTERS ============
typedef struct {
    void *ptr;
} finalize_data_t;

// ============ RECORDS ============
typedef struct {
    packet_header_t header;
    float distance;
    float final_altitude;
    float final_speed;
    float heading;
    finalize_data_t finalize;
} fly_straight_request_t;

typedef struct {
    packet_header_t header;
    bool success;
    int32_t error_code;
} fly_straight_response_t;

typedef struct {
    packet_header_t header;
    float distance;
    float final_altitude;
    float final_speed;
    float heading;
    finalize_data_t finalize;
} fly_arc_request_t;

typedef struct {
    packet_header_t header;
    bool success;
    int32_t error_code;
} fly_arc_response_t;

typedef struct {
    packet_header_t header;
    float final_altitude;
    float final_speed;
    finalize_data_t finalize;
} fly_up_request_t;

typedef struct {
    packet_header_t header;
    bool success;
    int32_t error_code;
} fly_up_response_t;

typedef struct {
    packet_header_t header;
    float final_altitude;
    float final_speed;
    finalize_data_t finalize;
} fly_down_request_t;

typedef struct {
    packet_header_t header;
    bool success;
    int32_t error_code;
} fly_down_response_t;

// ============ WIRE PACKETS ============
// Per-record packet types with schema info and source node

typedef struct {
    const char* schema_file;   // schema .h file name
    uint16_t    source_node;   // originating node ID
    uint8_t     index;         // record type index
    uint16_t    length;        // payload size
    fly_straight_request_t data;           // payload
} fly_straight_request_packet_t;

typedef struct {
    const char* schema_file;   // schema .h file name
    uint16_t    source_node;   // originating node ID
    uint8_t     index;         // record type index
    uint16_t    length;        // payload size
    fly_straight_response_t data;           // payload
} fly_straight_response_packet_t;

typedef struct {
    const char* schema_file;   // schema .h file name
    uint16_t    source_node;   // originating node ID
    uint8_t     index;         // record type index
    uint16_t    length;        // payload size
    fly_arc_request_t data;           // payload
} fly_arc_request_packet_t;

typedef struct {
    const char* schema_file;   // schema .h file name
    uint16_t    source_node;   // originating node ID
    uint8_t     index;         // record type index
    uint16_t    length;        // payload size
    fly_arc_response_t data;           // payload
} fly_arc_response_packet_t;

typedef struct {
    const char* schema_file;   // schema .h file name
    uint16_t    source_node;   // originating node ID
    uint8_t     index;         // record type index
    uint16_t    length;        // payload size
    fly_up_request_t data;           // payload
} fly_up_request_packet_t;

typedef struct {
    const char* schema_file;   // schema .h file name
    uint16_t    source_node;   // originating node ID
    uint8_t     index;         // record type index
    uint16_t    length;        // payload size
    fly_up_response_t data;           // payload
} fly_up_response_packet_t;

typedef struct {
    const char* schema_file;   // schema .h file name
    uint16_t    source_node;   // originating node ID
    uint8_t     index;         // record type index
    uint16_t    length;        // payload size
    fly_down_request_t data;           // payload
} fly_down_request_packet_t;

typedef struct {
    const char* schema_file;   // schema .h file name
    uint16_t    source_node;   // originating node ID
    uint8_t     index;         // record type index
    uint16_t    length;        // payload size
    fly_down_response_t data;           // payload
} fly_down_response_packet_t;

// Packet encode helpers - return pointer to data for direct population
static inline fly_straight_request_t* fly_straight_request_packet_encode(
        fly_straight_request_packet_t* pkt,
        uint16_t source_node)
{
    pkt->schema_file = DRONE_CONTROL_SCHEMA_FILE;
    pkt->source_node = source_node;
    pkt->index = 0;
    pkt->length = sizeof(fly_straight_request_t);
    return &pkt->data;
}

static inline fly_straight_response_t* fly_straight_response_packet_encode(
        fly_straight_response_packet_t* pkt,
        uint16_t source_node)
{
    pkt->schema_file = DRONE_CONTROL_SCHEMA_FILE;
    pkt->source_node = source_node;
    pkt->index = 1;
    pkt->length = sizeof(fly_straight_response_t);
    return &pkt->data;
}

static inline fly_arc_request_t* fly_arc_request_packet_encode(
        fly_arc_request_packet_t* pkt,
        uint16_t source_node)
{
    pkt->schema_file = DRONE_CONTROL_SCHEMA_FILE;
    pkt->source_node = source_node;
    pkt->index = 2;
    pkt->length = sizeof(fly_arc_request_t);
    return &pkt->data;
}

static inline fly_arc_response_t* fly_arc_response_packet_encode(
        fly_arc_response_packet_t* pkt,
        uint16_t source_node)
{
    pkt->schema_file = DRONE_CONTROL_SCHEMA_FILE;
    pkt->source_node = source_node;
    pkt->index = 3;
    pkt->length = sizeof(fly_arc_response_t);
    return &pkt->data;
}

static inline fly_up_request_t* fly_up_request_packet_encode(
        fly_up_request_packet_t* pkt,
        uint16_t source_node)
{
    pkt->schema_file = DRONE_CONTROL_SCHEMA_FILE;
    pkt->source_node = source_node;
    pkt->index = 4;
    pkt->length = sizeof(fly_up_request_t);
    return &pkt->data;
}

static inline fly_up_response_t* fly_up_response_packet_encode(
        fly_up_response_packet_t* pkt,
        uint16_t source_node)
{
    pkt->schema_file = DRONE_CONTROL_SCHEMA_FILE;
    pkt->source_node = source_node;
    pkt->index = 5;
    pkt->length = sizeof(fly_up_response_t);
    return &pkt->data;
}

static inline fly_down_request_t* fly_down_request_packet_encode(
        fly_down_request_packet_t* pkt,
        uint16_t source_node)
{
    pkt->schema_file = DRONE_CONTROL_SCHEMA_FILE;
    pkt->source_node = source_node;
    pkt->index = 6;
    pkt->length = sizeof(fly_down_request_t);
    return &pkt->data;
}

static inline fly_down_response_t* fly_down_response_packet_encode(
        fly_down_response_packet_t* pkt,
        uint16_t source_node)
{
    pkt->schema_file = DRONE_CONTROL_SCHEMA_FILE;
    pkt->source_node = source_node;
    pkt->index = 7;
    pkt->length = sizeof(fly_down_response_t);
    return &pkt->data;
}

// Packet verify helpers - validate and return data pointer
static inline const fly_straight_request_t* fly_straight_request_packet_verify(
        const void* packet_buffer,
        uint16_t* source_node)
{
    const fly_straight_request_packet_t* pkt = (const fly_straight_request_packet_t*)packet_buffer;
    
    // Verify schema file
    if (strcmp(pkt->schema_file, DRONE_CONTROL_SCHEMA_FILE) != 0) return NULL;
    
    // Verify packet type
    if (pkt->index != 0) return NULL;
    
    // Verify payload size
    if (pkt->length != sizeof(fly_straight_request_t)) return NULL;
    
    // Return source node (informational only)
    if (source_node) *source_node = pkt->source_node;
    
    return &pkt->data;
}

static inline const fly_straight_response_t* fly_straight_response_packet_verify(
        const void* packet_buffer,
        uint16_t* source_node)
{
    const fly_straight_response_packet_t* pkt = (const fly_straight_response_packet_t*)packet_buffer;
    
    // Verify schema file
    if (strcmp(pkt->schema_file, DRONE_CONTROL_SCHEMA_FILE) != 0) return NULL;
    
    // Verify packet type
    if (pkt->index != 1) return NULL;
    
    // Verify payload size
    if (pkt->length != sizeof(fly_straight_response_t)) return NULL;
    
    // Return source node (informational only)
    if (source_node) *source_node = pkt->source_node;
    
    return &pkt->data;
}

static inline const fly_arc_request_t* fly_arc_request_packet_verify(
        const void* packet_buffer,
        uint16_t* source_node)
{
    const fly_arc_request_packet_t* pkt = (const fly_arc_request_packet_t*)packet_buffer;
    
    // Verify schema file
    if (strcmp(pkt->schema_file, DRONE_CONTROL_SCHEMA_FILE) != 0) return NULL;
    
    // Verify packet type
    if (pkt->index != 2) return NULL;
    
    // Verify payload size
    if (pkt->length != sizeof(fly_arc_request_t)) return NULL;
    
    // Return source node (informational only)
    if (source_node) *source_node = pkt->source_node;
    
    return &pkt->data;
}

static inline const fly_arc_response_t* fly_arc_response_packet_verify(
        const void* packet_buffer,
        uint16_t* source_node)
{
    const fly_arc_response_packet_t* pkt = (const fly_arc_response_packet_t*)packet_buffer;
    
    // Verify schema file
    if (strcmp(pkt->schema_file, DRONE_CONTROL_SCHEMA_FILE) != 0) return NULL;
    
    // Verify packet type
    if (pkt->index != 3) return NULL;
    
    // Verify payload size
    if (pkt->length != sizeof(fly_arc_response_t)) return NULL;
    
    // Return source node (informational only)
    if (source_node) *source_node = pkt->source_node;
    
    return &pkt->data;
}

static inline const fly_up_request_t* fly_up_request_packet_verify(
        const void* packet_buffer,
        uint16_t* source_node)
{
    const fly_up_request_packet_t* pkt = (const fly_up_request_packet_t*)packet_buffer;
    
    // Verify schema file
    if (strcmp(pkt->schema_file, DRONE_CONTROL_SCHEMA_FILE) != 0) return NULL;
    
    // Verify packet type
    if (pkt->index != 4) return NULL;
    
    // Verify payload size
    if (pkt->length != sizeof(fly_up_request_t)) return NULL;
    
    // Return source node (informational only)
    if (source_node) *source_node = pkt->source_node;
    
    return &pkt->data;
}

static inline const fly_up_response_t* fly_up_response_packet_verify(
        const void* packet_buffer,
        uint16_t* source_node)
{
    const fly_up_response_packet_t* pkt = (const fly_up_response_packet_t*)packet_buffer;
    
    // Verify schema file
    if (strcmp(pkt->schema_file, DRONE_CONTROL_SCHEMA_FILE) != 0) return NULL;
    
    // Verify packet type
    if (pkt->index != 5) return NULL;
    
    // Verify payload size
    if (pkt->length != sizeof(fly_up_response_t)) return NULL;
    
    // Return source node (informational only)
    if (source_node) *source_node = pkt->source_node;
    
    return &pkt->data;
}

static inline const fly_down_request_t* fly_down_request_packet_verify(
        const void* packet_buffer,
        uint16_t* source_node)
{
    const fly_down_request_packet_t* pkt = (const fly_down_request_packet_t*)packet_buffer;
    
    // Verify schema file
    if (strcmp(pkt->schema_file, DRONE_CONTROL_SCHEMA_FILE) != 0) return NULL;
    
    // Verify packet type
    if (pkt->index != 6) return NULL;
    
    // Verify payload size
    if (pkt->length != sizeof(fly_down_request_t)) return NULL;
    
    // Return source node (informational only)
    if (source_node) *source_node = pkt->source_node;
    
    return &pkt->data;
}

static inline const fly_down_response_t* fly_down_response_packet_verify(
        const void* packet_buffer,
        uint16_t* source_node)
{
    const fly_down_response_packet_t* pkt = (const fly_down_response_packet_t*)packet_buffer;
    
    // Verify schema file
    if (strcmp(pkt->schema_file, DRONE_CONTROL_SCHEMA_FILE) != 0) return NULL;
    
    // Verify packet type
    if (pkt->index != 7) return NULL;
    
    // Verify payload size
    if (pkt->length != sizeof(fly_down_response_t)) return NULL;
    
    // Return source node (informational only)
    if (source_node) *source_node = pkt->source_node;
    
    return &pkt->data;
}


#ifdef __cplusplus
}
#endif
